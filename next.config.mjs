/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["https://postimg.cc/", "i.postimg.cc"],
  },
  trailingSlash: true,
  // output: "export",
};

export default nextConfig;
