/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "portalpopclub.com.br",
      "cdn.shopify.com",
      "www.nuevamujer.com",
      "example.com",
    ],
  },
};

export default nextConfig;
